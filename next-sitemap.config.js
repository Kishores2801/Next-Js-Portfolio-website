/** @type {import('next-sitemap').IConfig} */
const config = {
    siteUrl: 'kishore-portfolio-app.firebaseapp.com', // Replace with your site's URL
    generateRobotsTxt: true, // (optional)
  };
  
  module.exports = config;
  